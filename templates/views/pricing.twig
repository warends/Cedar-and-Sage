{% verbatim %}
<page-banner bg-image="../../img/bgs/pricing-banner.png"></page-banner>

<section id="pricing" class="padded-vert-2x">
  <div class="hero-text">
    <h1>packages</h1>
    <h3>pricing<span class="end-border">  _______</span></h3>
  </div>

  <div class="container">
    <div class="row">
      <div class="col-xs-12">
          <p class="caps forest text-center lead">In my business, I believe is is best to be as transparent as possible regarding cost and client expectations. I think that provides a great base for successful design experience. My goal is to get to know you and transform the way that you live through design.</p>
      </div>
    </div>

  	<div class="row">
  		<div ng-repeat="service in services">
  			<div class="col-sm-3 col-sm-offset-2 padded-top2x">
          <a ng-click="showSlide(service.slug)" class="pricing-box">
            <h5>{{service.name}}</h5>
    				<img ng-src="{{service.img}}" class="img-responsive" >
          </a>
  			</div>

        <div class="pricing-slide" ng-class="{show: selectedService === service.slug}">
          <h3 ng-click="close()" class="close">X</h3>
          <p class="padded-top2x">{{service.desc}}</p>
        </div>
  		</div>
  	</div>
  </div>
</section>

<div class="cta">
    <div class="container text-center">
        <div class="row">
            <div class="col-xs-12">
                <h3>Inspiration is the fuel to our creativity</h3>
            </div>
        </div>
    </div>
</div>
{% endverbatim %}
